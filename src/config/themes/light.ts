import { theme } from 'config/theme'

export default {
  backgroundColor: `${theme.colors.neutral[50]}`,
  textColor: `${theme.colors.gray[700]}`,
  headerBackgroundColor: `${theme.colors.neutral[100]}`,
}
