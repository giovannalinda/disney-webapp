import { theme } from 'config/theme'

export default {
  backgroundColor: `${theme.colors.neutral[200]}`,
  textColor: `${theme.colors.gray[700]}`,
  headerBackgroundColor: `${theme.colors.white}`,
  hover: `${theme.colors.gray[700]}`,
  scroll: `${theme.colors.white}`,
}
